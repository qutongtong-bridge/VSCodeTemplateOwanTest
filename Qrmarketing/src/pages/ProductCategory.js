import React from 'react';
import { Card, Empty } from 'antd';

const ProductCategory = () => {
  return (
    <Card title="产品参数设置">
      <Empty description="产品参数设置功能开发中" />
    </Card>
  );
};

export default ProductCategory;